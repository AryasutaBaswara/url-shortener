namespace: url-shortener-prod
resources:
  - ../../base
patches:
  - target:
      kind: Deployment
      name: url-shortener
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
images:
  - name: ghcr.io/aryasutabaswara/url-shortener
    newTag: bad-version
